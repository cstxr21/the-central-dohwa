> 🔒 **완료·배포 게이트 (모든 단계 md 공통 — 복붙 필수)**: 이 단계를 "완료"라고 말하기 전에, 이 문서의 `## 완료조건 (기계 판정)` 항목을 **마스터 `scripts/verify.mjs`로 실행**(`npm run verify`)해 **전부 PASS(GREEN)** 임을 출력으로 증명한다. 하나라도 FAIL·미구현·미측정·증거없음이면 **미완료** — "했다"고 말하지 말 것. auto 항목은 스크립트가 빌드결과·public·src를 읽어 검사, manual 항목은 `reports/manual-evidence.json`에 증거(수치·경로·요약)를 채워야 PASS. 마스터 verify 스크립트 원본·사용법은 **[11단계](일-11단계-배포점검.md) §마스터 검증 스크립트**에 있다. 신규 현장은 docs 프로세스 md 전체를 복붙 → 그 코드블록을 `scripts/verify.mjs`로 저장 → `content.ts`/`site.ts`만 그 현장 값으로 채우면 게이트가 자동으로 그 현장 기준 검사한다(라우트·슬러그 자동 감지). (현장 산출물 `이미지-배치표.md`는 복붙 대상 아님.)

> **작업 순서 1c/11 — 이미지·영상 큐레이션(자산 선별·분류)** · 전제: [1a단계](일-1a단계-정보추출.md)(추출 이미지·영상) · [1b](일-1b단계-검증보강.md)와 병렬(둘 다 1a 산출물 소비)
> **목적**: 1a가 누락 0으로 긁어온 이미지·영상 더미에서 **사이트에 실제 쓸 것만 한 장씩(영상은 한 편씩) 시각 정독으로 선별**하고, 용도별 디렉토리로 분류한다. 명백 쓰레기는 패턴으로 먼저 쳐낸다.
> **입력**: `<SAVE_PATH>/images`·`<SAVE_PATH>/videos`(+`ocr.json`·`manifest.json`) · **산출물**: 분류된 이미지·영상 디렉토리(`<SAVE_PATH>/curated/<category>/`) + `curation.json`(keep/drop·용도·페이지 매핑) + 버린목록 로그
> **다음 단계(2단계)로 넘길 것**: keep 확정 이미지·영상 + 페이지 매핑표. **2단계가 이미지는 webp 변환 + `next/image` + `public/images/`, 영상은 `public/videos/`(또는 포스터+`next/image`)로 사용**한다(여기선 선별·분류까지만, 변환·적용은 2단계 §7).
> **배포 점검(11단계)**: 미사용 이미지·깨진 이미지 참조 정리는 배포 직전 **[11단계](일-11단계-배포점검.md)**가 점검한다(누락/미사용 = 정리 대상).
> **적용**: 아래 ` ``` ` 프롬프트 블록을 AI에 전달하고 `<SAVE_PATH>`·`<DOMAIN_NOTE>`만 치환. 이 파일은 자기완결.

---

# 이미지·영상 큐레이션 프롬프트 (범용)

**대원칙**: **패턴으로 양을 줄이고(싸게) · 비전으로 정확도를 사고(한 장씩 정독) · 페이지 매핑으로 기준을 잡는다.**
판정 기준은 경로/파일명이 아니라 **그림 내용 + 어느 페이지 어느 섹션에 쓰느냐**.

---

## 프롬프트 (복사해서 사용)

```
[입력]
- 이미지: <SAVE_PATH>/images  (1a 산출물, 콘텐츠해시 파일명)
- 영상: <SAVE_PATH>/videos  (있으면. 1a 산출물, 콘텐츠해시 파일명)
- 참고: <SAVE_PATH>/ocr.json(이미지 속 텍스트), manifest.json(원본 URL 매핑)
- 도메인: <DOMAIN_NOTE> (예: 분양) — 아래 [분류 체계]를 이 성격의 페이지에 맞게 사용.

[작업]
- 1a 이미지·영상에서 사이트에 쓸 것만 선별·분류한다. 결과: curated/ 디렉토리 + curation.json + 버린목록.
- 원본(images/·videos/)은 보존한다. 큐레이션은 복사로 수행(이동·삭제 금지).

① 명백 쓰레기 패턴 제외 (싸게 — 비전 호출 절감)
- manifest의 원본 URL/파일명 기준으로 일괄 drop: VR 뷰어 자산(/vr/·panorama·skin·loading), UI 조각(IconButton·arrow·btn·icon·sprite), 추적/장식 더미.
- 치수/용량 보조: 한 변 50px 미만(아이콘)·고정 타일 치수는 drop 후보.
- ★자동 drop한 건 전부 버린목록 로그에 남긴다(개수·패턴·예시). 묵시적 누락 금지.

② 살아남은 후보를 한 장씩 시각 정독 (정확도 핵심)
- 후보 이미지를 **비전으로 한 장씩** 본다. 경로가 아니라 실제 내용으로 판정.
- 각 장에 대해 구조화 기록(자유서술 금지):
  {file, 내용유형, 글자포함(ocr 연계), 용도(어느 페이지/섹션), 판정(keep|drop|maybe), 사유}
  · 내용유형 예: 조감도/투시도·평면도·단지배치도·동호배치도·조경·커뮤니티·입지지도·인테리어컷·분양가/일정표·자격안내·로고·아이콘
- ⚠️ **'글자 포함'은 drop 사유가 아니다 — CG 인포그래픽 패널은 keep(절대 실수 금지)**: 분양의 정보는 대부분 **CG 인포그래픽 패널(프리미엄·사업개요·BENEFIT·입지안내도·동호배치도·커뮤니티·브랜드·평면도 등)에 텍스트로 박혀** 있다. 이 패널들은 **그 섹션의 핵심 비주얼이라 keep**(해당 category로 분류)한다. drop은 오직 ① 순수 UI조각·아이콘·로고비트 ② 외부광고·트래커 ③ **의미 없는 스톡 배경사진**('이미지컷' 워터마크만 있고 단지 정보 0인 인물/풍경컷) ④ 중복(dedup) ⑤ VR스킨/로딩자산 에만 적용. ★**텍스트가 박혔다는 이유로, 또는 '본문은 content.ts로 재현하니까'라는 이유로 정보 패널을 drop하지 말 것** — 패널을 버리면 그 섹션의 대표 비주얼까지 동시에 사라진다(과거 실수: 세로 프리미엄 CG 패널을 drop). 본문 텍스트를 HTML로 재현하는 것과 **원본 CG 패널을 섹션 이미지로 keep하는 것은 별개**다. (※ [3단계](일-3단계-전역규칙.md) '글자 위 이미지 금지' = **우리가 새 텍스트를 이미지 위에 얹지 말라**는 뜻이지, 텍스트가 인쇄된 원본 CG 자산을 버리라는 뜻이 아니다. '이미지컷 스톡사진'과 '정보 담긴 CG 패널'을 혼동 금지.)
- maybe는 사람 확인 큐로 따로 모은다(자동 확정 금지).

②' 영상(videos/)도 한 편씩 정독 (있을 때만)
- 각 영상을 실제로 분석한다: 대표 프레임(시작/중간/끝 + 장면 전환) 샘플링으로 내용 파악 + **자막·자료화면 속 텍스트 OCR** + 가능하면 내레이션/오디오로 fact 유무 확인.
- ★영상 속 텍스트(자막·타이틀·수치 인서트)는 이미지 OCR과 동일하게 취급 — 분양 fact(단지명·일정·수치·시설)가 박혀 있으면 ocr.json/검증 대상에 연계. 영상에만 있는 fact 누락 금지.
- 구조화 기록: {file, 길이/해상도, 내용유형(브랜드필름·조감 플라이스루·모델하우스 투어·드론 등), 글자포함(자막/인서트 텍스트 요약), 용도(어느 페이지/섹션 — 보통 메인 히어로/브랜드), 판정(keep|drop|maybe), 사유}.
- drop 예: 광고/외부 임베드 영상, 무음 짧은 루프 장식, 중복. keep는 사이트에 실제 쓸 배경/브랜드 영상.

③ 용도별 디렉토리 분류 (keep만 — 이미지·영상 공통)
- keep 이미지를 <SAVE_PATH>/curated/<category>/ 로 복사. **파일명은 `{단지영문슬러그}-{category/내용}-{NN}` 키워드 파일명으로 리네임(강제 — slug가 맨 앞, `category-slug` 순서 금지).** 9단계 [G9-IMGNAME] RED 기준(slug 접두 필수)과 일치시켜야 하며, `materialize-images.mjs`가 이 curation baseName으로 생성하므로 **이 이름이 곧 배포 파일명**(재실행해도 원복 안 되게 baseName을 키워드명으로 고정). `image/img/test`류 의미없는명 금지.
- 같은 그림 다른 해상도(PC/모바일)는 1차 1장 + variant 표기.

[분류 체계 — <DOMAIN_NOTE>=분양 예시 (2단계 섹션/이미지 배치에 대응 — 라우트 9개와 1:1 아님)]
  hero/        메인 히어로·대표 조감도(글자 없는 큰 컷)
  overview/    사업개요용 투시도·조감도
  location/    입지 지도·항공 조감(주변 맥락)
  premium/     프리미엄 서사 컷
  complex/     단지설계·조경·커뮤니티·배치도
  type/        타입별 평면도(84A/B/C 등) — 글자(면적) 포함, ocr 연계
  sales/       분양안내·일정/가격 도표(있으면)
  modelhouse/  모델하우스·인테리어컷·E모델하우스 스틸
  video/       브랜드필름·조감 플라이스루·모델하우스 투어 영상(+포스터 프레임)
  common/      로고·OG 이미지·공통
(다른 도메인이면 이 체계만 그 사이트 구조에 맞게 교체)

[출력 구조]
<SAVE_PATH>/
├─ curated/<category>/...        (keep 이미지·영상 복사본, 분류·리네임. 영상은 curated/video/)
├─ curation.json   [{file, kind(image|video), srcUrl, 내용유형, 글자포함, category, 용도, 판정, 사유}]
└─ curation-dropped.txt          (자동/수동 drop 목록 + 패턴·개수, 이미지·영상 공통)

[완료 조건 — curation.json·dropped.txt에 보고]
- images/ + videos/ 전수 1건도 빠짐없이 keep|drop|maybe 중 하나로 판정(미판정 0).
- keep 전부 category + 용도(페이지/섹션) 매핑 존재. 영상 keep은 내용유형·자막텍스트 요약 포함.
- 영상 속 fact(자막·인서트 수치)가 있으면 ocr.json/1b 검증 대상에 연계됐는지 명시(영상 전용 fact 누락 0).
- drop 전부 사유/패턴 기록(묵시적 누락 금지). maybe는 사람 확인 큐로 분리.
- ★**CG 인포그래픽 패널(프리미엄·사업개요·BENEFIT·입지안내도·동호배치도·커뮤니티·브랜드·평면도) drop 0건 재확인** — '글자 포함'·'본문 재현 예정'을 이유로 버린 정보 패널이 없는지 점검(있으면 keep으로 복구). drop은 UI조각·트래커·의미없는 스톡사진·중복·VR스킨에만.
- 원본 images/·videos/ 무변경(복사만). 콘텐츠해시 dedup 유지.
- keep 전부 curated/<category>/ 에 실제 복사 완료(파일 수 = curation.json keep 수).
```

---

## 참고 (블록 밖 — 사람용)

### 양에 따라 비중만 조절 (방법론은 동일)
- **적을 때(~50장 이하, 전수 정독 30분 내)**: ①은 최소화하고 **②(한 장씩 정독)를 사실상 전수**로. 화이트리스트가 제일 깔끔, 오탐 0.
- **많을 때(수백~수천)**: ①+치수+트리아지(글자/엣지 검출)로 **깔때기**를 만들어 양을 쳐내고, **비전 정독은 마지막 5%에만**. 안 그러면 정확도는 같아도 비용이 수십 배.
- 경계가 애매하면 "전수 육안이 30분 안에 끝나나?"로 가른다.

### 왜 비전 정독이 가장 정확한가
- "홈페이지에 쓸 수 있냐"는 **경로가 아니라 그림 내용 + 쓸 자리**로 판단할 문제. 패턴은 경향에 기댄 근사치라 예외에서 틀린다.
- 비전 정독은 keep/drop을 넘어 **내용유형·글자유무·용도(페이지 매핑)**까지 한 번에 뽑혀 2단계에 바로 쓰인다.

### 2단계와의 경계 (중복 금지)
- **1c**: 어떤 이미지·영상을 **keep/drop 하고 어느 분류·페이지로** (선별·매핑).
- **2단계 §7**: 그 keep 이미지를 **webp 변환 + `next/image` 적용 + `public/images/` 배치 + OG 이미지**, 영상은 **`public/videos/` 배치(또는 포스터 프레임 + lazy load)**.
- 즉 1c는 "무엇을", 2단계는 "어떻게 넣을지". 영상은 용량이 크니 2단계에서 압축/포스터/지연로드 정책 적용.

### 원천 원칙
- **원본 보존**: `images/`는 추출 원본 보관용, 사이트용은 `curated/`로 따로. 이동·삭제 금지(복사).
- **버린 건 로그**: 자동 제거가 늘수록 "뭘 버렸나" 추적이 중요. 묵시적 누락 금지.
- **현장 결과는 산출물에**: 방법론만 이 문서(범용), 현장별 실제 keep/drop·분류는 `<SAVE_PATH>/curation.json`에.

---

## 완료조건 (기계 판정)

> 형식 `[Gxx-KEY] 설명 | auto|manual`. 마스터 `scripts/verify.mjs`가 이 `[Gxx-*]`를 파싱해 **구현·PASS를 강제**(이 문서에 ID를 선언했는데 스크립트에 검사가 없으면 = RED / 검사는 있는데 어느 md도 선언 안 하면 = 경고). **전부 GREEN 전엔 이 단계 "완료" 금지.**

- [G1C-CURATION] images/videos 전수 keep|drop|maybe 판정(미판정 0)·keep 매핑·keep baseName `{slug}-{category}-{NN}` 키워드명(slug 접두·의미없는명 0 — 9단계 G9-IMGNAME과 일치) | manual


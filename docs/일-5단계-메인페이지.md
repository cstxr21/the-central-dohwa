> 🔒 **완료·배포 게이트 (모든 단계 md 공통 — 복붙 필수)**: 이 단계를 "완료"라고 말하기 전에, 이 문서의 `## 완료조건 (기계 판정)` 항목을 **마스터 `scripts/verify.mjs`로 실행**(`npm run verify`)해 **전부 PASS(GREEN)** 임을 출력으로 증명한다. 하나라도 FAIL·미구현·미측정·증거없음이면 **미완료** — "했다"고 말하지 말 것. auto 항목은 스크립트가 빌드결과·public·src를 읽어 검사, manual 항목은 `reports/manual-evidence.json`에 증거(수치·경로·요약)를 채워야 PASS. 마스터 verify 스크립트 원본·사용법은 **[11단계](일-11단계-배포점검.md) §마스터 검증 스크립트**에 있다. 신규 현장은 docs 프로세스 md 전체를 복붙 → 그 코드블록을 `scripts/verify.mjs`로 저장 → `content.ts`/`site.ts`만 그 현장 값으로 채우면 게이트가 자동으로 그 현장 기준 검사한다(라우트·슬러그 자동 감지). (현장 산출물 `이미지-배치표.md`는 복붙 대상 아님.)

# 5단계 — 메인페이지 (`/`)

> **작업 순서 5/11** · 전제: 2·3·4a·4b단계 · 자기완결(UI §2.1 + IA 메인페이지 원문 포함)
> **목적**: 첫 화면 + 전체 단지 요약 + 관심고객등록 전환.
> **산출물**: `/`(`HomeHero`+`MagBlock`×N+분양소식/FAQ+관심고객등록 폼+ABOUT+다크 푸터)
> **배포 점검(11단계)**: 메인 관심고객등록 폼 실동작·view-source fact·미사용 정리는 **[11단계](일-11단계-배포점검.md)**가 점검(fact가 CSR로만 = blocker).

---

## 메타 (IA 정본 값)

- **TITLE** `{현장명} | 모델하우스 방문예약·분양안내` (네이버 30자 이내, READ-ONLY)
- **OG:TITLE** `{현장명} — {위치} {사업유형} {N}세대`
- **META DESC**(≤80자) `{현장명} 모델하우스 방문예약·분양안내. {위치} {N}세대 사업개요·입지·프리미엄·분양상담 안내.`
- **H1** `{현장명}`
- **H2**: 인트로 `{브랜드 컨셉 한 줄}` + 섹션 주제어(사업개요 · 입지환경 및 미래가치 · 프리미엄 · 단지안내 · 평면도 · 분양안내 · 모델하우스 · 분양소식 · 자주 묻는 질문 · 관심고객등록) + ABOUT `한눈에 보는 단지 정보`. (※ 섹션 H2 = NAV 주제어 라벨 그대로 / ABOUT H2도 **현장명 접두어 생략** — 브랜드는 H1·본문이 받침, 아래 ⚠️ 일관.)
  - ⚠️ **메인 섹션 H2는 주제어만**(브랜드 접두어 제거) — 브랜드는 H1·title·JSON-LD·ABOUT·본문이 받침. 서브가 "{현장명}+주제" 롱테일을 소유하므로 메인 H2 브랜드 반복은 카니발.

## 콘텐츠 원칙

- **분량**: 본문 fact 800~2,000자(정보 밀도 우선).
- **메인 = 요약 허브**: 각 섹션(사업개요·입지·프리미엄·단지·평면도·분양·모델하우스·소식·FAQ)을 **1블록 요약 teaser + 서브 링크**로만. 상세·동일 워딩은 서브에. 한 섹션에 다른 영역 콘텐츠를 펼치지 말 것.
- **이미지**: Hero·MagBlock 이미지는 **실제 비율 무크롭**(잘림 0)으로 가용 비주얼 최대 활용.
- ★**near-duplicate 금지(전 구간 공통)**: 섹션 간·메인↔서브에 같은 문장 복붙 금지. 같은 fact라도 워딩을 바꾼다. (이 문서에서 가장 자주 어기는 규칙)
- ⚠️ **청약 종료 후엔 '지난 청약 일정' 나열 금지(미분양 인식 유발)**: 정규 청약(공고·특공·1·2순위·당첨·정당계약)이 끝났으면 지난 날짜를 열거하지 말고 **"청약 접수 마감 · 선착순 동·호 지정 계약 진행 중"**으로 간결·긍정 표기 — **분양 teaser·FAQ·ABOUT 공통**. 입주자모집공고일은 **분양가 기준일**로만 유지(일정 나열용 아님). 선착순 진행은 1차 출처 검증 시에만 단정(본 건 = 헤럴드경제 2025.10.31 "선착순 동호 지정 계약 중" 보도). 상세 정본·`/sales` 분기 = [7단계 §분양 단계 전환](일-7단계-서브-분양-모델하우스.md).

## 구성 순서 (UI §2.1 정본)

**Hero → 인트로(브랜드 컨셉) → 섹션 매거진 블록(01~0N) → 분양소식 → FAQ → 관심고객등록(폼) → ABOUT(정보성) → 다크 푸터.**
- 관심고객등록 폼 = **하단부 최종 전환 CTA**(순서상 ABOUT 직전 · 실제 **푸터 바로 위 마지막은 ABOUT** 정보블록).
- 분양소식·FAQ는 **2단 카드 또는 풀폭 분리** 둘 다 허용(분양소식이 featured+요약으로 두꺼우면 분리가 균형에 유리).
- 배경 `bg-surface`(흰)↔`bg-background`(아이보리) 교차, **같은 색 인접 금지**, 마지막 **다크 푸터(`bg-ink`)**.

### Hero (`HomeHero`)

**[공통]**
- **높이**: 데스크탑 `lg:h-screen` · 모바일 `min-h-[calc(100svh_-_56px)]`(콜바 56px 제외, `svh`로 주소창 출렁임 방지). ⚠️ **arbitrary 값 언더스코어 필수**(`100svh_-_56px`) — 빠지면 CSS 무효 → 높이 0으로 콘텐츠가 상단에 몰림(실발생 버그).
- **헤드는 차분한 배경(하늘·여백) 위**. 타워·디테일 위에 큰 글자 금지(가독·우아함 손해).
- **상단 공백 방치 금지** — 카피·핵심지표로 균형.
- 헤더: 최상단 투명+흰 글자 → 스크롤 24px↑ `bg-ivory/95` 솔리드. **밝은 하늘 위 흰 nav용 상단 스크림** 확보.
- 카피: eyebrow(영문 장식, 너무 얇게 금지) → 한글 헤드(serif 대형) → 메타(입지·세대수 fact 한 줄) → CTA. 상단 좌측 에디토리얼 또는 가운데.
- 핵심지표(세대수·전용·입주·역) 노출하되 **무거운 하단 불투명 바 금지** → 헤어라인 행.
- 히어로 CTA = **관심고객 등록(`/modelhouse#reservation`) + 전화 버튼(`tel:`{전화} — 아이콘+번호만, 텍스트 라벨 없음) 2개 나란히**, **모바일·태블릿 숨김**(콜바 중복, `hidden min-[1360px]:flex`).
- 50대 가독성·`prefers-reduced-motion` 정지컷 폴백.

**매체 선택 (이미지 / 영상)** — 제약이 달라 나눈다(안 나누면 "빈 상단·영상 모션 가림" 실수 반복).
- **[A] 이미지(정적)**: `next/image` priority+webp. 텍스트 배치 자유(풀 오버레이·듀오톤). 생동감은 은은한 Ken Burns까지.
- **[B] 영상**: 자산 `public/videos/hero/hero-main.mp4` + poster=영상 첫 프레임 webp, 자동재생 `muted playsinline loop`. **압축·폴백은 [3단계](일-3단계-전역규칙.md) §B 영상 규칙**(1280폭·`-an`·`+faststart`·≤1MB·저전력/저모션 시 poster). ⚠️ **모션 존(보통 하단 1/3)을 바·스크림으로 덮지 말 것**(영상이 정지처럼 보임) — 스크림은 텍스트 쪽만. **심리스 루프(시작=끝)**.

✅ **본 프로젝트 최종 확정 = [A] 이미지 히어로**: 원본 단지 조감 CG `img_intro.webp` 한 장(선명) + 느린 Ken Burns(scale 1.04→1.09 / 32s) + 텍스트 clip-path 와이프(`hero-wipe`) + 라이트 스윕 1회(`hero-sweep`).
- ⚠️ **시도→폐기(되풀이 금지)**: ① **추상 보케 영상** = 히어로에 단지가 안 보여 반려(첫 화면엔 반드시 단지). ② **필름 그레인** = 밝은 대낮 CG 위에선 노이즈/압축깨짐처럼 보여 고급감↓. ③ **2.5D 패럴랙스(하늘/건물 누끼 분리)** = 누끼 잔영 가리려 뒤 하늘을 blur → 정지 시 하늘이 뿌옇게 노출돼 화질↓.
- 교훈: **선명한 단지 CG + 은은한 Ken Burns가 상한.** 정지 CG의 시각 품질 > 인위적 모션.

### 섹션 매거진 블록 (`MagBlock` 01~0N)

- 각 섹션 = **이미지 + 숫자 인덱스(`01 — Overview`, 브론즈 이태릭) + 한글 H2(serif 26~34px) + fact 문단 + 자연어 링크**를 **한 블록**으로. 좌우 교차(`reverse`) 2단. **이미지엔 링크·hover 줌 없음**(이동은 `linkLabel`로만).
- **fact 문단 = 2~3문장 티저**(슬로건 한 줄 금지). 검증 fact만, 미확정·운영계획은 단정 금지("안내"/"예정").
- ★**"풍성하되 어뷰징 0"(실수 잦은 지점)**:
  - **풍성함 = 정보 밀도**: 각 섹션에 그 영역 검증 fact 1~2개 이상 구체 포함(개요=세대수·층수·일반분양, 타입=주택형별 세대수·면적, 분양=공급 조건·선착순 단계·입주[**분양가 금액은 메인 미기입 → `/sales` 정본**, 아래 ⚠️ 참조], 모델하우스=주소·관람시간). 슬로건/형용사로 길이만 늘리지 말 것.
  - **어뷰징 0** (규칙 원천 = [3단계 ★전역 가드 ⟨어뷰징⟩] 준수): 메인 티저는 특히 과장 수식어("최고급"·"초역세권")·도배·near-duplicate·채우기 문장 0.
  - 점검: 각 티저가 **검증 fact 포함 + 서로 다른 워딩 + 과장·반복 0** 이면 통과.
- ⚠️ **분양가 상세 숫자는 `/sales` 정본에만 — 메인엔 분양가 금액 미기입**: 분양 티저·ABOUT·FAQ 미리보기 어디에도 분양가 범위/금액(억·만 원·원)을 적지 말 것. 메인 분양 티저는 "분양가·공급 조건을 입주자모집공고 기준으로 안내" + **linkLabel `분양가·일정 안내` → `/sales`** 링크만. (분양안내에 상세 입력 시 near-duplicate·키워드 카니발 방지.) ABOUT은 "자세한 금액은 분양안내에서 확인"으로 링크 유도.
- **이미지 잘림 방지**: 고정 `aspect-[4/3]`+`object-cover`로 묶으면 다 잘림 → 컨테이너 비율=이미지 실제 비율(`style={{aspectRatio:w/h}}`) 또는 튀는 세로 컷은 가로 이미지로 교체.
- ⚠️ **평면도·분양안내·모델하우스 섹션 이미지는 사용자 직접 첨부 예정 → 임의 이미지 삽입 금지(절대 실수 금지)**: 이 3개 섹션은 MagBlock `placeholder` prop으로 **빈 중립 플레이스홀더 박스**("이미지 준비 중 · 사용자 첨부 예정", `border-dashed`)를 렌더한다. **자동 큐레이션·임의 이미지 삽입·재생성 금지.** 사용자가 실제 이미지를 줄 때까지 플레이스홀더 유지(다른 섹션 이미지 작업 시에도 이 3개는 손대지 말 것). 실제 이미지 수령 시 → `src/lib/images.ts` HOME에 항목 추가 + `page.tsx`에서 `placeholder` 제거하고 `image`/`ratio`/`alt` 연결.

### ★ 위치/지도 안내 — `MapLinks` 길찾기 버튼 미사용
- 현장(단지) 위치는 **입지환경 MagBlock 이미지(현장 지도 CG) + `/location`**, 견본주택 위치는 **모델하우스 위치안내 + 주소 텍스트**로 안내.
- ⚠️ **외부 지도앱 길찾기 버튼(`MapLinks`)은 전 페이지 미사용** — 이탈로 전환(방문예약)에서 벗어나고 SEO·AI 유입 기여가 0. 주소·전화는 텍스트 + `LocalBusiness` JSON-LD([7단계](일-7단계-서브-분양-모델하우스.md))로 구조화 노출.

### 분양소식 / FAQ (`lib/news.ts` · `lib/faq.ts` 단일 소스)
- **분양소식**(`news.ts`): 홈은 **발행글(POSTS) 우선 + 언론 보도(PRESS) 병행** 2단 — 좌측 발행글 최신 3편(카테고리·날짜·제목·요약, **`/news/[slug]` 내부링크**), 우측 언론 최신 N건(매체·날짜·제목, **외부 원문** `target=_blank rel=noopener`). 발행글 0편이면 보도 단독. 우상단 더보기 → `/news`. (전체는 8단계)
- **FAQ**(`faq.ts`): 홈은 **대표 질문+간단답 미리보기** + 전체 → `/faq`.
  - ★**미리보기 5개 권장(3개로 줄이지 말 것)**: 정보성 Q&A가 많을수록 롱테일 표면↑·체류↑·이탈↓. 단 **분양가 등 숫자 상세가 `/sales` 정본인 항목의 질문은 홈 미리보기에서 제외**(메인 분양가 숫자 미노출 정책과 일치 — 예: `FAQS.filter(f=>!f.q.includes("분양가")).slice(0,5)`). 전체 답변은 `/faq`에서 노출.
  - ★**답변 = 티저와 다른 워딩**(Q&A 톤 + 티저에 없는 추가 정보: 세대수=조합원 제외 맥락, 주택형=A·B 평면차+타입 링크, 입주=개별 통보, 청약=잔여세대 상담). 티저 복붙 금지.
  - ⚠️ **FAQPage JSON-LD는 `/faq`에만**(홈 미노출 — 스키마 중복 방지, [3단계](일-3단계-전역규칙.md) §F).
- 답변·요약은 1b 검증 fact만(불확실 = "입주자모집공고 기준 추후 안내", 창작 금지). 보도는 출처(매체·날짜·원문) 정확히.

### 관심고객등록 섹션 (`bg-background`, 푸터 바로 위)
- **좌 — 정본 구성(순서대로)**: ① eyebrow `Register` + H2 `관심고객등록` → ② 안내문(`{현장명}의 {핵심안내 키워드} 등 핵심 안내를 관심고객으로 등록하신 분께 가장 먼저 전해 드립니다. 성함과 연락처만 남겨 주시면 분양 상담실에서 빠르게 연락드려 안내합니다.`) → ③ 혜택 리스트 4개(브론즈 ✓): `{단계별 1번 항목}` / `전용 {주택형}㎡ 분양가·공급 정보` / `모델하우스(견본주택) 방문 예약` / `1:1 맞춤 분양 상담` → ④ 📍**견본주택**(주소 + `관람 {운영시간} · 사전 예약 관람`) → ⑤ 📞**분양문의**(큰 serif `{전화}` + `선 예약 후 방문 안내`).
  - ⚠️ **②·③의 "청약" 표현은 분양 단계 따라 분기(절대 실수 금지)**: **선착순 분양 중이면** ②`핵심안내 키워드`=`분양가·잔여 세대·선착순 계약 정보`, ③ 1번 항목=`선착순 계약·잔여 세대 현황 안내`로 — **"청약 일정"을 혜택으로 광고하지 말 것**(청약은 이미 마감 → 미분양·시점 오인). 청약 진행 중일 때만 `청약 일정·입주자모집공고 안내` 사용. [선착순 = 청약 마감 간결 표기 규칙](#콘텐츠-원칙) 준수.
  - ⚠️ 길찾기 버튼(`MapLinks`) 미사용 — 견본주택 위치는 📍주소 텍스트로만 안내(외부 지도앱 이탈 방지).
  - ⚠️ `GRAND OPEN {날짜}`는 1차 출처 확정 시만 — 미확정/이미 오픈이면 날짜 창작 금지 → `관람 {운영시간} · 사전 예약 관람`(운영시간 기본 10:00~18:00, [7단계](일-7단계-서브-분양-모델하우스.md)).
  - 좌측이 우측 폼보다 짧아 **중간 횡함 방지** — 위 ①~⑤로 폼 높이를 채우고, 그래도 짧으면 `flex h-full flex-col justify-center`(`justify-between`은 중간 공백 → 지양).
- **우 — 흰 폼카드**(`border-line`, 폼 규정=4b단계, eyebrow `Reservation Form`/title/note 헤더).

### ABOUT 블록 (정보성, 푸터 바로 위 마지막)
- eyebrow 영문 `About This Page` + `한눈에 보는 단지 정보` H2(**현장명 접두어 생략** — H2 주제어 일관, 브랜드는 H1·첫 문단이 받침) + **자연어 3문단**(위치·규모·입지·설계 / 타입·특화·시공 / 공급·일정·문의). 확정 fact만 800~2,000자, 브랜드명 문단당 1회(첫 문단은 `{현장명}…`로 시작해 AI 인용 fact 연결). Thin Content 방지 + AI 인용 1차 후보.

## 내부링크 전략 (전환 동선)

> 9단계 [내부링크 최적화]의 현장 적용 정본. 전역 nav는 9개 표면을 잇지만, **SEO·전환 핵심은 본문 자연어 동선**이다.
- **표준 퍼널**: 입지환경 → 평면도 → 분양안내 → 모델하우스 → **관심고객등록(/modelhouse#reservation)**. 모든 콘텐츠 페이지는 About 말미에서 분양안내·관심고객등록으로 수렴.
- **메인**: 5개 서브(개요·입지·프리미엄·단지·평면도) MagBlock 링크 + 분양안내·모델하우스 링크 + 관심고객등록 폼 = 전 표면 허브.
- **자연어 앵커 2개+/페이지**("84A 평면도 보기"·"분양가 안내"). "여기 클릭"·중복 도배 금지.

## 완료 조건 (사람용 요약)

> 기계 강제는 같은 문서의 `## 완료조건 (기계 판정)` 게이트가 담당(`npm run verify`). 아래는 그 단계 spec·사람 점검용 읽기 목록 — 게이트가 자동 검사하는 항목은 게이트가 최종 판정, 자동으로 못 잡는 것만 사람이 증거로 확인.

- [ ] 구성 순서대로(이중 구조 0, 배경 교차, 마지막 다크 푸터) / 관심고객등록 폼 = 하단 최종 CTA(ABOUT 직전) · ABOUT = 푸터 바로 위 마지막
- [ ] Hero: 모바일 `calc(100svh_-_56px)`(**언더스코어**), 헤드가 차분한 영역 위·상단 공백 균형, CTA 모바일 숨김 / (영상 채택 시) 모션 존 비가림·심리스 루프·poster=첫 프레임
- [ ] MagBlock 이미지 잘림 0 + **2~3문장 티저**(검증 fact 1~2개 구체 + 서브와 다른 워딩)
- [ ] **풍성하되 어뷰징 0**: 모든 티저 ① 검증 fact 포함 ② 과장 수식어 0 ③ 키워드·브랜드 도배 0 ④ near-duplicate 0 ⑤ 채우기 0 / 미확정 = "입주자모집공고 기준 추후 안내"
- [ ] 길찾기 버튼(`MapLinks`) 미사용 — 위치는 입지환경 MagBlock 이미지·모델하우스 주소 텍스트 + `LocalBusiness` JSON-LD로만
- [ ] 분양소식 = 발행글 우선+보도 병행(0편이면 보도 단독) / FAQ 미리보기 5개 + **FAQPage 스키마 `/faq`에만**
- [ ] **선착순 분양 진행 중이면 → "청약 마감 · 선착순 (동·호 지정) 계약 진행 중"으로 간결 표기**: 지난 청약 일정(공고·특공·1·2순위·당첨·정당계약 날짜) 나열 0건(분양 teaser·FAQ·ABOUT 전부). 모집공고일은 분양가 기준일로만. **(절대 실수 금지)**
- [ ] ABOUT 정보성 800~2,000자 + 내부링크 전환 동선(메인=전 표면 허브, 본문 앵커·도배 0)

---

## 완료조건 (기계 판정)

> 형식 `[Gxx-KEY] 설명 | auto|manual`. 마스터 `scripts/verify.mjs`가 이 `[Gxx-*]`를 파싱해 **구현·PASS를 강제**(이 문서에 ID를 선언했는데 스크립트에 검사가 없으면 = RED / 검사는 있는데 어느 md도 선언 안 하면 = 경고). **전부 GREEN 전엔 이 단계 "완료" 금지.**

- [G5-ABOUT] 메인 'About This Page' 정보성 섹션 존재 | auto
- [G5-SCHEDULE] 지난 청약 일정 나열 0·미분양 urgency 0(선착순 정식표기 OK) | auto


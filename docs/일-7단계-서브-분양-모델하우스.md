> 🔒 **완료·배포 게이트 (모든 단계 md 공통 — 복붙 필수)**: 이 단계를 "완료"라고 말하기 전에, 이 문서의 `## 완료조건 (기계 판정)` 항목을 **마스터 `scripts/verify.mjs`로 실행**(`npm run verify`)해 **전부 PASS(GREEN)** 임을 출력으로 증명한다. 하나라도 FAIL·미구현·미측정·증거없음이면 **미완료** — "했다"고 말하지 말 것. auto 항목은 스크립트가 빌드결과·public·src를 읽어 검사, manual 항목은 `reports/manual-evidence.json`에 증거(수치·경로·요약)를 채워야 PASS. 마스터 verify 스크립트 원본·사용법은 **[11단계](일-11단계-배포점검.md) §마스터 검증 스크립트**에 있다. 신규 현장은 docs 프로세스 md 전체를 복붙 → 그 코드블록을 `scripts/verify.mjs`로 저장 → `content.ts`/`site.ts`만 그 현장 값으로 채우면 게이트가 자동으로 그 현장 기준 검사한다(라우트·슬러그 자동 감지). (현장 산출물 `이미지-배치표.md`는 복붙 대상 아님.)

# 7단계 — 서브페이지: 분양안내 · 모델하우스

> **작업 순서 7/11** · 전제: 2·3·4a·4b단계 · 자기완결(IA 각 페이지 + UI §2.7~2.8 원문 포함)
> **대상 2개**: 분양안내 `/sales` · 모델하우스 `/modelhouse`
> **공통 전제**: 미확정(분양가·청약·일정·계약)은 전부 **"입주자모집공고 시점 기준 추후 안내"**, 수치 창작 0, "최저가/확정/직결" 단정·비교 금지(표시광고법), 핵심 정보 17px+ ink 500.
> **이미지·밀도 원칙**: [6단계 §공통 원칙](일-6단계-서브-단지정보.md) 동일 적용 — 큐레이션 이미지(특히 modelhouse 인테리어/유닛 갤러리) **최대 활용**(keep된 컷 전부 배치, 1~2장 방치 금지), **세로 인포그래픽/세로컷은 무크롭**(width/height intrinsic+h-auto), 이미지 임베드 명칭(유닛·평형·시설)은 **OCR 검증값만**(창작 0).
> ⚠️ **[필수 게이트] 적용**: [6단계 §공통 원칙 게이트](일-6단계-서브-단지정보.md#공통-원칙--본문-밀도이미지-활용-thin-페이지-금지) 동일 — 작성 전 카테고리 이미지 **전수 확인** → **[분류표](이미지-배치표.md) 기록** → **완료조건 항목별 ✅ 증명**. 안 본 이미지 제외·near-dup 추측 제외 금지.
> **배포 점검(11단계)**: 분양가·일정 시점 표기와 광고심의필·시행/시공 3주체 등 표시광고법 표기가 빌드에 살아있는지는 배포 직전 **[11단계](일-11단계-배포점검.md)**가 확인한다(누락 = blocker).

---

## 페이지별 섹션 → 콘텐츠 매핑 + 구성 순서 (영역 적합성)

| 페이지 | 구성 순서(섹션) | 들어가면 안 되는 것 |
|---|---|---|
| **분양안내 `/sales`** | 히어로 → 인트로 → 공급개요 표 → 분양가 → 청약 자격 → 분양 일정 → 계약 조건 → 사업주체 → 유의사항 → 분양상담 → About | 평면 상세(→평면도 요약+링크), 단지 조경/커뮤니티(→단지안내) |
| **모델하우스 `/modelhouse`** | 히어로 → 2단(좌 위치·전화 / 우 예약폼) → 위치안내 표 → **유닛·인테리어 갤러리** → 관람 3STEP → 분양상담 → 관심고객등록 → FAQ 링크 → About | 분양가 상세(→분양안내), 평면 상세(→평면도) |

> ⚠️ **분양가·청약·일정 상세는 `/sales`에만**(평면도·프리미엄 등 다른 페이지는 요약+링크 — near-duplicate 금지). **인테리어/견본주택 갤러리는 `/modelhouse`에만**(평면도는 텍스트 링크만, 동일 이미지 두 페이지 중복 게재 금지).

> **내부링크 시스템 = [6단계 §내부링크 시스템(심플)](일-6단계-서브-단지정보.md) 정본 적용**: **카드 블록·매트릭스 없음**(전역 nav가 상호연결). 본문 맥락 링크 **≤2/페이지**(퍼널 앞 또는 disambiguation, 동일 URL 1회), **About 링크 0**, 하단 CTA 1개(모델하우스는 자체 예약폼), BreadcrumbList JSON-LD, orphan 0. 표준 퍼널 종착 두 페이지.

---

## 분양안내 `/sales` (모티프 `doc`)

- **TITLE** `{현장명} 분양안내 | 분양가·청약·일정` · **DESC**(≤80) `{현장명} 분양가, 청약 자격, 분양 일정과 공급 조건을 입주자모집공고 기준으로 안내합니다.`
- **OG:TITLE** `{현장명} 분양 — {N}세대 · {청약자격요약} {사업유형}`
- **H1** `{현장명} 분양안내` · **H2** 공급 개요(표: 단지명·대지위치·건축규모·세대수·전용면적) · 분양가 안내 · 청약 자격 · 분양 일정 · 계약 조건 · 사업주체 · 유의사항 · 분양 상담.
  - 분양가·청약·일정·계약 미공개 시 전부 **"입주자모집공고 시점 기준 추후 안내"** — 수치 창작 0. **분양가 공개 시 `Residence`+`AggregateOffer`(또는 `Offer`)** 생성, 미공개면 Offer 없이 `Residence` additionalProperty에 "분양가·청약·일정 = 추후 안내". '최저가/확정/직결' 단정·비교 금지.
- UI: **분양가·일정 미공개 시(흔함)** 아코디언 대신 **공급 개요 표 + 분양가/청약/일정/계약 "입주자모집공고 시점 기준 추후 안내" 섹션**(2×2 그리드 등). 공개 시 아코디언(보더 없이 `border-line` divider, 활성 시 브론즈 1px underline + 제목 굵기 500, 색만 의존 X). 표는 `LineSpec`로 통일, 값 16px+ ink.
- **분양 단계 전환(청약 종료 → 선착순)**: 정규 청약(특공·1·2순위·정당계약)이 **종료되면** 분양안내 페이지는 **선착순(잔여세대) 안내로 전환**한다. 운영자 설정 fact(`SUBSCRIPTION_CLOSED`/`SALES_PHASE` = content.ts)로 분기:
  - `SUBSCRIPTION_CLOSED=true` → **"정규 청약 종료 · 현재 선착순 분양 중"** 한 문장 + 선착순 장점(청약통장·순위 무관 동·호지정 계약 가능)만 간단히. **지나간 청약 일정(과거 날짜) 표는 노출하지 않는다** — 종료된 날짜 목록은 미분양 인식을 줌(반드시 제거).
  - `false`(청약 진행 중) → "청약 일정" 표(공고·특공·1·2순위·발표·계약) 노출, 접수 단계 안내.
  - ⚠️ **미분양 인식 주는 표현/요소 금지**: 지나간 청약 일정 표·"지난 청약 일정(종료)" 제목·"잔여세대·소진에 따라 변동" 반복 = 부정 인식 → 쓰지 말 것. "선착순 분양 중"으로 능동·긍정 프레이밍. 허위 마감·"완판/최저가/확정" 단정 금지, 분양가는 입주자모집공고 시점 표기 유지.
- **About**(eyebrow `About This Page`) `분양 한눈에 보기` 공급({N}세대 {전용면적}) / 분양가·청약 자격·분양 일정은 입주자모집공고 기준 추후(시점 표기 의무) / 분양 상담 {전화}·관심고객등록. ※"최저가/확정/직결" 단정·비교 금지. 권장 1,000~2,000자.
- **대표 비주얼(이미지 0 방지)**: 분양안내는 표(분양가·일정·공급개요) 중심이지만 **상단에 단지 대표 외관 1컷(풀와이드)** 을 둔다. 분양안내 전용 이미지 카테고리는 보통 없으므로(원본 분양가·청약은 PDF·표), **미사용 자산(예: 메인 히어로용 조감 CG 중 미사용분)을 `public/images/sales/`로 재활용** — 단, 다른 페이지에 쓰인 이미지와 **중복 금지(near-dup)**. 평면·조경·커뮤니티 컷은 넣지 않음(각 영역 페이지로). ※ `public/images/<cat>`는 1c 큐레이션 카테고리별 생성이라 sales·news·faq 등 무(無)카테고리 페이지는 dir이 없을 수 있음 → 대표 비주얼 필요 시 위처럼 수동 보강.

## 모델하우스 `/modelhouse` (모티프 `gallery`)

- **TITLE** `{현장명} 모델하우스 | 방문예약 및 분양상담` · **DESC**(≤80) `{현장명} 모델하우스(현장에 따라 갤러리·홍보관 등) 방문예약 안내. {모델하우스주소} 위치·관람 예약 방법·분양상담 {전화}.`
- **H1** `{현장명} 모델하우스` · **H2** 방문예약(히어로 다음 2단 좌측 제목) · 모델하우스 위치안내(표) · **유닛·인테리어 갤러리**(견본주택 컷) · 관람 예약 방법(3 STEP) · 분양상담 신청 · 관심고객등록 · 자주 묻는 질문(→ /faq).
- **유닛·인테리어 갤러리(이미지 최대 활용)**: modelhouse category로 keep된 견본주택 컷을 **전부** 유닛별(예: 84A·84B 등)로 묶어 그리드/풀와이드로 노출. 세로 인테리어 컷은 무크롭. 캡션은 검증된 유닛명만(연출용 가구·유상옵션 ※주석).
- ⚠️ **E-모델하우스/360° VR은 실제 자산(VR 투어·파노라마)이 확인될 때만 표기·링크·임베드**. 자산이 없으면 "E-모델하우스/360° VR" 문구 자체를 넣지 말 것(없는 기능 안내 = 허위). VR 자산이 없는 현장은 견본주택 인테리어 갤러리만 노출.
- **페이지 구성 = 히어로 다음 2단 레이아웃**(참조 스크린샷 정본 · 별도 인트로 없음 = 좌측 소개가 인트로):
  - **좌 — Modelhouse Information**(영문 eyebrow): 제목(H2 `{현장명} 모델하우스`) + `견본주택 · {주소}` + **소개 문단**(현장에서 평면·커뮤니티·특화를 직접 경험 → 온라인 방문예약 신청 권유, 따뜻한 톤) + 📍**위치 안내**({주소} + "방문 시 사전 예약 권장") + 📞**방문·분양 문의**(큰 serif {전화} + "선예약 후 방문 안내 · 동·호/잔여 현황 상담"). ⚠️ **길찾기 버튼(`MapLinks`) 미사용** — 위치는 `견본주택 · {주소}` 텍스트 + 아래 위치안내 표로 안내하고, 주소·전화는 아래 `LocalBusiness`/`RealEstateAgent` JSON-LD로 구조화 노출(외부 지도앱 이탈 방지 — 방문 동선은 예약 폼으로 수렴).
  - ★**방문예약 혜택 ✓4불릿**(소개 문단과 📍위치 사이 배치, 브론즈 ✓): **이 폼이 전 표면 CTA의 종착**(헤더·히어로·콜바·서브 PageOutro 모두 `/modelhouse#reservation`로 수렴)이고 앵커 직행 시 갤러리·3STEP 등 설득 컨텍스트를 건너뛰고 착지하므로, 좌측을 혜택 불릿으로 채워 **"왜 지금 방문예약 하나"를 폼 옆에서 설득 + 좌측 높이로 횡(중간 공백) 방지**. ⚠️ **메인 관심고객등록 혜택과 다른 워딩 필수**(같은 `ContactForm` 재사용 → near-duplicate 금지): 메인 = "등록 시 **받는 정보**"(분양가·잔여 세대 우선 안내) 톤 ↔ 여기 = "방문 시 **직접 경험·확인**하는 것" 톤. 예시: `실물 견본주택에서 평면·마감재 직접 확인` / `커뮤니티·특화 설계 현장 체험` / `동·호 지정·잔여 세대 현황 즉시 상담` / `1:1 맞춤 분양 상담 + 방문 일정 조율`. 검증 fact 기반·과장 수식어 0.
  - **우 — Reservation Form**(영문 eyebrow): **eyebrow `Reservation Form` + 제목 `방문예약 신청` + 안내문 `담당자가 확인 후 연락드려 방문 일정을 확정합니다`** 를 폼 상단에 두고, 그 아래 폼(성함·연락처·연락 가능한 시간대·개인정보 동의·제출, 폼 규정 = 4b단계). 폼 컴포넌트에 `eyebrow/title/note` prop으로 주입(메인 관심고객등록 폼도 동일 패턴 재사용). ⚠️ **앵커는 이 폼에 직접 부여**: `id="reservation"`+`scroll-mt-24`는 2단 래퍼·좌측 컬럼이 아니라 **우측 폼 컴포넌트(eyebrow/title/inputs 묶음)** 에 건다. 모바일(2단 세로 스택)에서 헤더·히어로·콜바·PageOutro 어느 CTA를 눌러도 좌측 정보가 아니라 **폼 입력부로 직행**해야 하며, `scroll-mt-24`로 고정 헤더 아래에 폼 제목+첫 입력칸이 보이게 착지(전 브레이크포인트 헤더 높이 보정). ❌ 2단 섹션/좌측 컬럼에 `id` 부여 금지 = '폼 윗부분 착지' 버그.
  - **좌우 영문 eyebrow 대칭**: 좌 `Modelhouse Information` ↔ 우 `Reservation Form`(브론즈 accent italic). ⚠️ **'GRAND OPEN {날짜}' 등 오픈일은 1차 출처로 확정된 경우만**; 미확정·이미 선착순 단계면 날짜 창작 금지 → "방문 시 사전 예약 권장"으로 대체.
  - 이후: 위치안내 표(갤러리·현장·분양상담) → 관람 예약 방법 **3 STEP** → 분양상담 → 관심고객등록 → FAQ 링크 → About.
- ★**모델하우스(견본주택) 운영(관람) 시간 = 10:00~18:00**(분양 견본주택 **범용 기본값**, `content.ts` `modelHouse.hours`). 현장이 별도 시간을 고지하면 그 값으로 교체하되, 없으면 이 10~18시를 쓴다.
  - ⚠️ **혼동 금지(과거 실수)**: 입주자모집공고·계약안내의 **계약/유상옵션 방문 시간(예: 10:00~17:00)**을 견본주택 **관람 시간**으로 가져오지 말 것. 둘은 별개다(계약 이벤트 시간 ≠ 상시 관람 시간). 관람 시간은 위 범용 기본 또는 현장 고지값.
- **JSON-LD** = `LocalBusiness` 또는 `RealEstateAgent` — **주소 공개 시 `address`(PostalAddress)·`telephone` 필수 포함**(가능하면 `geo`), 운영시간 확정 시 `openingHoursSpecification`(미확정이면 생략). 페이지에 보이는 주소·전화와 **1:1 일치**(미노출 정보 schema 단독 표기 금지). 주소 미공개면 `LocalBusiness` 생성 금지(창작 0). **NAP(이름·주소·전화) 구조화 데이터는 이 페이지 1곳에만**(다른 페이지 복제 금지 — 중복 schema 방지).
- 일정·주소·연락처는 핵심 정보(17px+ ink 500).
- **About**(eyebrow `About This Page`) `모델하우스 한눈에 보기` 모델하우스({모델하우스주소}·오픈 예정) / 방문예약 3단계 / 분양 상담 {전화} 2문단. 권장 1,000~2,000자.

## 완료 조건 (사람용 요약)

> 기계 강제는 같은 문서의 `## 완료조건 (기계 판정)` 게이트가 담당(`npm run verify`). 아래는 그 단계 spec·사람 점검용 읽기 목록 — 게이트가 자동 검사하는 항목은 게이트가 최종 판정, 자동으로 못 잡는 것만 사람이 증거로 확인.

- [ ] 2페이지 TITLE/DESC/H1/H2/About 적용, `{플레이스홀더}` 0
- [ ] 분양가 미공개 시 `Offer` 미생성 + "추후 안내" 표기, 단정·비교 표현 0
- [ ] **청약 종료 시 선착순 전환(긍정 프레이밍)**: `SUBSCRIPTION_CLOSED=true`면 "정규 청약 종료 · 선착순 분양 중" 간단 표기(청약통장·순위 무관 계약). **지나간 청약 일정 표 미노출** + "잔여세대·소진·지난 일정(종료)" 등 미분양 인식 표현 0, 허위 마감·단정 0
- [ ] 모델하우스 2단 레이아웃 + 위치·전화 텍스트(길찾기 버튼 0) + 예약 폼 + `id="reservation"`(**우측 폼 컴포넌트에 직접** · `scroll-mt-24`) — 모바일 2단 세로 스택 시 좌측 정보 아닌 **폼 입력부로 착지**
- [ ] **관심고객/방문예약 라인 카피**: 좌 소개문단(현장 경험→방문예약 권유)·**방문예약 혜택 ✓4불릿(메인 관심고객등록과 다른 워딩 — "방문 시 직접 경험·확인" 톤, near-duplicate 0)**·위치(사전예약 권장)·전화(선예약 안내) + 우 폼 `Reservation Form` eyebrow·`방문예약 신청` 제목·확인 연락 안내문. 좌우 영문 eyebrow 대칭. 오픈일 미확정 시 날짜 창작 0
- [ ] **이미지 최대 활용**: modelhouse category keep 컷 전부 갤러리 배치(미사용분 사유 명시), 세로 인테리어 컷 무크롭(글자/구도 잘림 0), 캡션 명칭 OCR 검증값만(창작 0)
- [ ] 모티프 doc/gallery, 핵심 정보 17px+ ink 500
- [ ] **내부링크 전환 동선**: /sales → 타입·단지 + 관심고객등록, /modelhouse → 분양안내·타입 + 예약폼. 본문 자연어 앵커 2종+·orphan 0·도배 0
- [ ] (모델하우스 주소 공개 시) `LocalBusiness`/`RealEstateAgent` JSON-LD 생성 — `address`(PostalAddress)·`telephone` **필수 포함**(운영시간 확정 시 `openingHoursSpecification`), 페이지 노출값과 1:1 / 미공개 시 미생성(창작 금지) / NAP은 이 페이지 1곳에만(복제 0)
- [ ] **본문 충실화([3단계](일-3단계-전역규칙.md) §F)**: 각 About 1,000~2,000자 **측정 충족** + 공급개요 표·내부링크, 어뷰징 0(‘최저가/확정/직결’ 단정·비교 금지 유지)

---

## 완료조건 (기계 판정)

> 형식 `[Gxx-KEY] 설명 | auto|manual`. 마스터 `scripts/verify.mjs`가 이 `[Gxx-*]`를 파싱해 **구현·PASS를 강제**(이 문서에 ID를 선언했는데 스크립트에 검사가 없으면 = RED / 검사는 있는데 어느 md도 선언 안 하면 = 경고). **전부 GREEN 전엔 이 단계 "완료" 금지.**

- [G7-FORM] 모델하우스: 예약 anchor(#reservation)·처리방침 링크·api/contact route | auto
- [G7-SCHEDULE] sales·modelhouse 지난 청약 일정 나열 0·미분양 urgency 0 | auto


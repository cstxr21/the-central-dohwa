> 🔒 **완료·배포 게이트 (모든 단계 md 공통 — 복붙 필수)**: 이 단계를 "완료"라고 말하기 전에, 이 문서의 `## 완료조건 (기계 판정)` 항목을 **마스터 `scripts/verify.mjs`로 실행**(`npm run verify`)해 **전부 PASS(GREEN)** 임을 출력으로 증명한다. 하나라도 FAIL·미구현·미측정·증거없음이면 **미완료** — "했다"고 말하지 말 것. auto 항목은 스크립트가 빌드결과·public·src를 읽어 검사, manual 항목은 `reports/manual-evidence.json`에 증거(수치·경로·요약)를 채워야 PASS. 마스터 verify 스크립트 원본·사용법은 **[11단계](일-11단계-배포점검.md) §마스터 검증 스크립트**에 있다. 신규 현장은 docs 프로세스 md 전체를 복붙 → 그 코드블록을 `scripts/verify.mjs`로 저장 → `content.ts`/`site.ts`만 그 현장 값으로 채우면 게이트가 자동으로 그 현장 기준 검사한다(라우트·슬러그 자동 감지). (현장 산출물 `이미지-배치표.md`는 복붙 대상 아님.)

# 6단계 — 서브페이지: 단지 정보 계열

> **작업 순서 6/11** · 전제: 2·3·4a·4b단계 · 자기완결(IA 각 페이지 + UI §2.2~2.6 원문 포함)
> **대상 5개**: 사업개요 `/overview` · 입지환경 `/location` · 프리미엄 `/premium` · 단지안내 `/complex` · 평면도 `/floorplan`
> **공통 전제**: 서브 구조 `히어로 → 인트로 → 섹션`(4a단계), PageHero 모티프, 미확정은 "입주자모집공고 기준 추후 안내", FAQ 섹션 두지 않음(/faq 일원화), 핵심 정보 17px+ ink 500.
> **배포 점검(11단계)**: 이 페이지들의 fact·내부링크·이미지가 빌드에 살아있는지는 배포 직전 **[11단계](일-11단계-배포점검.md)**가 회귀 점검한다.

---

## 공통 원칙 — 본문 밀도·이미지 활용 (thin 페이지 금지)

> ⚠️ **[필수 게이트] 이미지 전수 확인 → 분류표 → 완료조건 증명 (안 지키면 작업 무효 · 6·7·8단계 공통)**
> 1. **전수 확인**: 페이지 작성 전 해당 카테고리 `public/images/<cat>/`의 **모든 이미지를 빠짐없이 직접 열어** 내용을 파악한다(추측·샘플링·파일명만으로 판단 금지). **텍스트가 박힌 보드(공식 브로슈어 패널 등)는 OCR 가독 크기로 확인**(너무 길면 잘라서라도 전부 읽는다).
> 2. **분류표 작성·갱신**: 이미지별로 `배치 페이지 · 사용 여부 · (미사용 시) 사유 1줄`을 **[docs/이미지-배치표.md](이미지-배치표.md)**에 기록한다. **콘텐츠·정보가 있는 검증 자산(공식 브로슈어 보드·도면·지도·시설 컷)은 사용이 default**, 제외는 예외(사유 필수). "안 봤으니 뺀다"·"near-dup일 것 같아서 뺀다"는 **사유로 불인정**.
> 3. **완료조건 증명**: 각 페이지 완료 시 아래 [완료 조건](#완료-조건-사람용-요약)을 **항목별 ✅로 증명**(특히 "콘텐츠 이미지 전부 배치 / 1~2장 방치 0"). 미충족이면 미완료.
> ⚠️ **필러 금지(억지 배치 = 어뷰징)**: "이미지 최대 활용"은 **콘텐츠/정보가 있는 자산** 한정이다. **정보 가치 없는 장식·배경(추상 구름/색면 밴드·빈 배경·단순 텍스처 등)은 채우려고 억지로 넣지 말고 `미사용(사유: 장식·정보 없음)`으로 분류**한다. 즉 "검증 자산 default 사용"과 "필러 금지"를 함께 지킨다 — **정보 있으면 쓰고, 장식뿐이면 뺀다.** (채우기용 이미지 = '채우기' 어뷰징과 동일.)
> ※ **near-duplicate 금지 = "같은 이미지를 두 페이지에 중복 게재" 금지**일 뿐, **한 페이지 안에서 콘텐츠 이미지를 적게 쓰는 핑계가 아니다.** 텍스트 워딩의 near-dup만 피하면, 검증 콘텐츠 이미지는 최대한 다 쓴다.

> 분양원본은 대개 **콘텐츠가 이미지(CG)에 임베드**돼 있고 추출 텍스트는 면책 문구뿐이다. 따라서 서브페이지는 "이미지를 크게·많이 보여주고, 검증된 fact로 H2를 채우는" 구조가 기본이다. 빈약하게 만들지 말 것.

- **큐레이션 이미지 최대 활용** — 해당 섹션(category)으로 keep된 이미지는 **원칙적으로 전부 배치**(풀와이드 쇼케이스·갤러리·카드·밴드로 소진). 1~2장만 쓰고 방치 금지. 미사용분은 완료 시 그 사유를 명시.
- **세로 인포그래픽 패널 = 풀와이드 무크롭** — 글자/도면이 박힌 세로 긴 이미지(조경·커뮤니티·평면도·입지안내도 등, 가로세로비 < 1)는 `next/image`의 `width`/`height` intrinsic + `h-auto w-full`로 **자르지 않고** 크게 노출. `fill`+`object-cover` 크롭 금지(글자 잘림). 가로비 이미지만 aspectRatio 컨테이너+object-cover 허용.
- **H2 섹션 다수 + 하단 About** — 섹션을 충분히 쌓고(각 SectionHeading) 페이지 끝에 `AboutThisPage`(SEO 자연어 본문). 분량은 [3단계 §F](일-3단계-전역규칙.md) 기준.
- **이미지 속 텍스트는 OCR 검증값만** — 시설명·정원/광장명·동별 층수·룸 구성 등 이미지에 박힌 정보는 `extraction/ocr.json` 등 1차 검증값만 사용. **없는 명칭 창작 절대 금지**(예: 임의 정원명). 확정 fact는 [content.ts 단일소스](일-2단계-디자인시스템.md).
- **권장 섹션 순서(이미지 우선)** — 인트로 리드(1~2문장) → 대표 비주얼(풀와이드) → 핵심 fact 섹션 다수(H2 + 이미지/인포그래픽 동반, 원본 사이트의 서브구조 순서를 따름) → 하단 `AboutThisPage`.
- **섹션 영역 적합성 ≠ 이미지 축소** — 각 페이지는 **그 페이지 주제에 맞는 이미지를 전부 활용**(갤러리·풀와이드·범례). "대표 1컷만 남기고 줄이기" 금지. 단, **주제와 무관한 섹션을 끼워넣지 말 것**(적합성은 지키되 이미지 수는 최대).

### 페이지별 섹션 → 콘텐츠 매핑 (영역 적합성 — 이대로 배치)

> ⚠️ **단지 외관 CG(조감도·투시도)는 사업개요·단지안내가 나눠 갖는다 — 같은 이미지를 두 페이지에 중복 게재하지 말 것(near-duplicate=어뷰징).** 대표 **조감(항공)**은 사업개요 오프닝 쇼케이스, **투시도(보행자 시점)**는 단지안내. 분할해 **각 이미지는 한 페이지에만**(반복 실수 지점).

| 페이지 | 들어가는 콘텐츠 | 들어가면 안 되는 것 |
|---|---|---|
| **사업개요 `/overview`** | **대표 단지 조감(항공) 2~3컷 쇼케이스**·사업 개요 LineSpec(표)·건축 규모·사업주체·**법정 고지(이동통신설비 등)** | 투시도 갤러리(→단지안내), 조경·커뮤니티·배치도, 단지안내와 **동일 이미지 중복** |
| **입지환경 `/location`** | 입지 지도·교통·반도체클러스터·공원/하천·도시개발·생활인프라·입지안내도 (입지 강점 상세는 **여기만**) | 단지 조감·평면도, 길찾기 버튼(`MapLinks` 미사용 — 위치는 입지 지도·안내도 이미지로) |
| **프리미엄 `/premium`** | **단지/브랜드 차별화**(브랜드·국민평형·특화설계·커뮤니티 요약·미래가치). 입지·단지는 **요약 1~2줄+링크만** | 평면도·배치도, **입지/단지 상세 워딩 복제(→입지환경·단지안내, near-duplicate 금지)** |
| **단지안내 `/complex`** | **단지 투시도**·단지설계·단지조경·그리너리 라운지(커뮤니티)·단지배치도·동호배치도 | 평면 상세(→평면도), 분양가(→분양안내) |
| **평면도 `/floorplan`** | 주택형 표·평면도·평면 구성(룸)·옵션 | 단지 배치/조경 본문, **견본주택 인테리어 갤러리(→모델하우스, 텍스트 링크만)** |

> ⚠️ **프리미엄 ≠ 입지환경 복제**: 프리미엄은 "이 단지를 왜 사는가"(브랜드·상품·종합 요약), 입지환경은 "입지 fact 상세". 같은 셀링포인트를 두 페이지에 같은 워딩으로 펼치면 near-duplicate(어뷰징). 프리미엄은 요약+링크, 상세는 해당 페이지에 1곳만.
> ⚠️ **인테리어/견본주택 컷은 모델하우스 전용**(7단계). 평면도 페이지는 평면·면적·옵션까지, 인테리어는 텍스트 링크로만(이미지 중복 게재 금지).

- 이미지 카테고리(`public/images/<cat>`)는 **표시 위치**와 다를 수 있다 — 원본 파일명 카테고리가 아니라 **콘텐츠 주제**로 배치 페이지를 정한다(예: `images/overview/*` 조감컷은 파일명이 overview여도 "단지 조감"이므로 `/complex`에 배치).

---

## 사업개요 `/overview` (모티프 `circle`)

- **TITLE** `{현장명} 사업개요 | 공급정보 안내` · **DESC**(≤80) `{현장명} 사업개요. {위치} {규모} {N}세대, {전용면적}, 시행시공 {시행사} 안내.`
- **H1** `{현장명} 사업개요` · **H2**(H1 중복 제거·fact 키워드) 브랜드 스토리 · 건축 설계({설계사}) · 사업 개요 — {N}세대 · {전용면적} · {규모}.
- 위치·대지·건축규모·공급규모·사업주체 등 fact = **`LineSpec` 표**(개별 H2 X):
  ```
  01 / 단지명          {현장명}
  02 / 위치            {주소}
  03 / 건축규모        지하 N층 ~ 지상 N층 (예시)
  04 / 총 세대수       {N세대 (일반분양 N세대)}
  05 / 전용면적        {59~155㎡, N개 주택형}
  06 / 입주 예정       {YYYY년 M월}
  ```
  - `LineSpec`: 좌 브론즈 인덱스(`font-accent` 15px) + `text-muted` 라벨 15px + 우 값. 값(dd) **`text-ink` 17~18px·weight 500**, 자간 안 벌림, 행 사이 `border-line`, `py-5`.
- **About**(eyebrow `About This Page`) `사업개요 한눈에 보기` 자연어 2~3문단(확정 fact만): 위치·규모 / 주택형·설계 / 사업주체·안내(분양가·일정은 입주자모집공고 기준 추후, 상담 {전화}). 권장 800~1,500자.

## 입지환경 `/location` (모티프 `map`)

- **TITLE** `{현장명} 입지환경 | 교통·생활·개발호재` · **DESC**(≤80) `{현장명} 입지환경. {역세권/개발}, {대표역}·{인근역} 교통과 생활인프라·개발호재·미래가치를 안내합니다.`
- **H1** `{현장명} 입지환경` · **H2**(지역·교통 키워드 보강) {역세권} 교통환경 — {대표역}·{인근역}({노선}) · 생활인프라 · 의료시설 · 쇼핑·문화시설 · 개발호재 · {권역} 미래가치.
- 인근 별개 단지·시설은 **주변 환경**으로만(혼동 방지). 보도 인용 출처 명시, 호재 "예정/추진".
- UI: **지도** 모노톤 커스텀, 마커 1개만 브론즈 나머지 회색, 줌·드래그 미니멀. **거리/시간** 큰 숫자(`font-accent` 60~80px) + 단위 한글 "분" + 역명 16px+ ink(영문 MIN 써도 한글 병기). **개발호재** 세로 타임라인(좌 hairline+도트, 연도 영문 이태릭→제목 serif 20px+→본문 산세리프 17px+ muted).
- **About**(eyebrow `About This Page`) `입지 한눈에 보기` 위치 / 교통({대표역}·{인근역}·{광역교통 예정}) / 개발호재(출처) 2~3문단. 권장 1,000~2,000자.

## 프리미엄 `/premium` (모티프 `bloom`)

- **TITLE** `{현장명} 프리미엄 | {차별화 서비스 요약}` · **DESC**(≤80) `{현장명} 프리미엄. {핵심 서비스 3~4개}를 안내합니다.`
- **H1** `{현장명} 프리미엄` · **H2** {서비스1}·{서비스2}·{서비스3}·{서비스4}·{라이프/트렌드}·제휴 파트너. 운영 계획에 따라 변경 가능 — 단정 금지 ※주석.
- UI: 풀블리드 이미지 + 카피 교차, 형용사 최소·사실/수치로. 캡션·설명 한글 16px+ ink/muted.
- **About**(eyebrow `About This Page`) `프리미엄 한눈에 보기` 핵심 서비스·컨셉 2~3문단. 권장 1,000~2,000자.

## 단지안내 `/complex` (모티프 `plan`)

- **TITLE** `{현장명} 단지안내 | 배치도·조경·커뮤니티` · **DESC**(≤80) `{현장명} 단지안내. {N}세대 배치도, {조경}, {커뮤니티 요약} 커뮤니티 시설을 안내합니다.`
- **H1** `{현장명} 단지안내` · **H2** 단지배치도 — {N}세대({전용면적}) · {조경설계} 조경계획 · 커뮤니티 시설({요약}) · 특화 공간 · 보안 시스템·입주민 서비스. 커뮤니티는 **H3 분류**(다이닝·액티비티·라운지 등). **이 페이지 FAQ 금지**(/faq 일원화).
- UI: 커뮤니티/시설 = 카드 그리드 X → 풀블리드 가로 갤러리(스냅 스크롤), 각 시설 큰 이미지 + 하단 캡션(한글 시설명 16px **주** + 작은 영문 보조).
- **About**(eyebrow `About This Page`) `단지 한눈에 보기` 규모({규모}·{N}세대)·구성(조경·커뮤니티 중심) 2~3문단 + CG 고지. 권장 1,000~2,000자.

## 평면도 `/floorplan` (모티프 `unit`)

- **TITLE** `{현장명} 평면도 | 전용 {전용면적} {타입수}개 주택형` · **DESC**(≤80) `{현장명} 평면도. {전용면적} {타입수}개 주택형({N}세대) 단위세대 평면도·면적 구성·동 배치를 안내합니다.`
- **H1** `{현장명} 평면도` · **H2**(확정 정보만 · **`평면도`·`전용면적` 등 검색 키워드를 자연 삽입** — ⚠️현장명 접두어·키워드 나열 금지) 전용면적 구성(예: `59·75·84㎡ 5개 주택형`) · 타입별 평면도(예: `59·75A·75B·84A·84B㎡`, └ 도면 미공개 시 H3 전용 NN㎡별 placeholder + "평면도 준비중") · 공간설계 특화 · 안내.
  - 네이버 검색은 메뉴 라벨보다 **title·H2·이미지 alt** 가중치가 크지만, 검색 수요가 큰 `평면도` 키워드를 **메뉴·URL·H1·title·alt 전부에서 일치**시키기 위해 메뉴 라벨=`평면도`, URL=`/floorplan`으로 통일함(2026-06 결정). title(`…평면도 | …`)·H2·alt(`{현장명} NN㎡ 평면도`)에도 평면도·전용면적 키워드를 노출.
- UI: 좌측 sticky 인덱스/상단 칩 `59A·59B·84A…`(타입명 16px+, active=브론즈+굵기 500, 색만 의존 X). 평면도 라인드로잉(치수·실명 14px+, 도면 위에서도 또렷). hover 정보는 **탭으로도** 열림. 마감재: 브랜드명 장식 영문, **재료·적용 위치 설명 한글 16px+ ink**.
  - ✅ **타입별 평면도 정본 = 탭(칩) 전환 + 기본 노출**(`PlanTabs` 클라이언트 컴포넌트): 상단 칩(59㎡A·59㎡B·84㎡·44㎡ OA, active=브론즈)으로 주택형 전환, **선택된 평면도는 클릭 없이 기본 노출**(첫 타입 자동 선택). ⚠️ **평면도엔 "크게 보기" 라이트박스 두지 말 것** — `unit0N.webp` 패널이 풀폭(`w-full h-auto`)으로 이미 충분히 크게 보임(클릭 확대 불필요). 선택 타입의 전용·공급·세대수 한 줄 표기. (마감재 리스트 등 작은 보드만 라이트박스 사용.)
- **평면도 미공개 시(흔함)**: 타입별 **'평면도 준비중' placeholder(`type-NN.svg`)**를 H3(전용 NN㎡) 그리드에 + 전용면적 카드 요약. **평형·구성·마감재·치수 창작 금지**(Helpful Content·표시광고법). 도면 공개 시 교체.
- **About**(eyebrow `About This Page`) `주택형 한눈에 보기` {전용면적} {타입수}개 주택형, 평면도 추후 공개 2문단. 권장(공개 전) 600~1,000자.

## 내부링크 시스템 (심플 통일 — SEO·AI 유입 최적) — 6·7·8 공통 정본

> **전제: 전역 헤더 nav + 다크 푸터 nav가 9개 페이지를 모두 상호연결**한다(크롤·접근·orphan 0 이미 보장). 그러므로 서브에 **"관련 페이지" 카드 블록·링크 매트릭스를 따로 만들지 않는다**(= nav 중복·사이트와이드 보일러플레이트 링크 → 가중치 희석). **과거 카드 매트릭스(`RelatedPages`)·인바운드 카운팅 방식은 폐기.**

### 구조 — 4층, 그 이상 만들지 않는다
1. **전역 nav + 푸터** = 전체 상호연결·크롤 동선(이미 존재). **서브에서 재현 금지.**
2. **Breadcrumb JSON-LD** = 계층 신호. 전 서브 `홈 / {제목}`(PageHero `path` 자동). 중복 생성 금지.
3. **본문 맥락 링크 = 페이지당 0~2개**(강제 아님 — 그 문장에서 **진짜 자연스러운 곳에만** 인라인). **같은 URL 1회**, 서술형 앵커("84A 평면도"·"분양가·일정"·"견본주택 위치"). **한 문장에 몰기·"여기 클릭"·동일 앵커 반복 0.** 방향은 **퍼널 앞으로**(입지→평면도→분양→모델하우스) 또는 **혼동 방지 1개**(예: 입지=현장 vs 모델하우스=견본주택).
4. **하단 전환 CTA = 관심고객 등록 버튼 1개**(`PageOutro` → `/modelhouse#reservation`). 그 외 버튼·중복 CTA 금지.

### About("한눈에 보기") = 링크 0, AI 인용 최적 요약 — 6·7·8 공통 정본
- ★**제목 형식 = eyebrow 영문 `About This Page` + H2 `{주제} 한눈에 보기`**(`SectionHeading as="h2" en="About This Page"`). ⚠️ **About H2엔 현장명 접두어 넣지 말 것** — 현장명은 H1·title·OG·breadcrumb·본문 첫 문단이 이미 받침. 7개 서브 About에 현장명을 또 박으면 **브랜드 H2 반복=키워드 스터핑(어뷰징)** → 주제어만 쓴다. (eyebrow는 장식 영문이라 SEO 가중치·어뷰징 무관, 통일성용.)
- **링크 0**(순수 요약). **한 문장 = 한 fact**(역명·시설 등 entity + 세대수·면적·층수·연도 등 수치 명시)로 **자기완결**, **첫 문단만 `{현장명}…`로 시작**해 AI 인용 fact를 브랜드와 연결. → 네이버 AI·검색 인용에 그대로 쓰이는 단락.

### AI 유입(GEO) 우선 원칙
- **AI 인용은 링크 수가 아니라 fact 밀도·자기완결성으로 결정**된다. 내부 동선은 nav·breadcrumb로 충분하니, 본문·About는 **링크 최소·fact 최대**로 간다.
- 링크가 문장을 끊지 않게 — 인용 단위(문장)는 깨끗하게 유지.

### 측정형 완료 게이트 (자동 점검 — 미충족 시 미완료)
- [ ] **카드 블록·링크 매트릭스 0**(`RelatedPages` 미사용)
- [ ] 본문 맥락 링크 **≤2/페이지** · **같은 URL 1회**(콘텐츠 링크 중복 0, 전환 CTA는 예외)
- [ ] **About 섹션 링크 0**(순수 요약)
- [ ] **하단 CTA 1개**(관심고객 등록) · **BreadcrumbList JSON-LD** 전 서브
- [ ] orphan 0(전역 nav 보장) · 홈에서 ≤2클릭

## 완료 조건 (사람용 요약)

> 기계 강제는 같은 문서의 `## 완료조건 (기계 판정)` 게이트가 담당(`npm run verify`). 아래는 그 단계 spec·사람 점검용 읽기 목록 — 게이트가 자동 검사하는 항목은 게이트가 최종 판정, 자동으로 못 잡는 것만 사람이 증거로 확인.

- [ ] 5페이지 TITLE/DESC/H1/H2/About 적용, `{플레이스홀더}` 0
- [ ] PageHero 모티프 circle/map/bloom/plan/unit
- [ ] 핵심 정보 17px+ ink 500, 이미지 잘림 0, FAQ 섹션 0
- [ ] **이미지 최대 활용**: 각 페이지 섹션 category로 keep된 이미지 전부 배치(미사용분은 사유 명시). 1~2장만 쓰고 방치 0
- [ ] **세로 인포그래픽 무크롭**: 가로세로비<1 이미지는 width/height intrinsic+h-auto(글자 잘림 0). object-cover 크롭으로 도면/카피 손실 0
- [ ] 미공개 항목 창작 0("추후 안내"/"준비중") + **이미지 임베드 명칭(시설/정원/광장/룸/층수)은 OCR 검증값만**(창작 0)
- [ ] 평면도 미공개 타입은 placeholder 자산(type-NN.svg 등) 생성·삽입(빈 영역 방지)
- [ ] **본문 충실화([3단계](일-3단계-전역규칙.md) §F)**: 각 페이지 **본문 전체**(리드+섹션 카피+About) 권장 분량 **측정 충족** — overview 800~1,500 / 입지·프리미엄·단지 1,000~2,000 / 평면도(공개 전) 600~1,000자(※About 블록만이 아니라 페이지 본문 합산) + 의도형 H2·fact 표/목록, 어뷰징 0
- [ ] **내부링크 시스템(§내부링크 시스템 — 심플)**: **카드 블록·링크 매트릭스 0**(전역 nav가 상호연결) · 본문 맥락 링크 **≤2/페이지**·동일 URL 1회 · **About 섹션 링크 0** · **하단 CTA 1개**(관심고객 등록) · **BreadcrumbList JSON-LD** 전 서브 · orphan 0(전역 nav 보장)

---

## 완료조건 (기계 판정)

> 형식 `[Gxx-KEY] 설명 | auto|manual`. 마스터 `scripts/verify.mjs`가 이 `[Gxx-*]`를 파싱해 **구현·PASS를 강제**(이 문서에 ID를 선언했는데 스크립트에 검사가 없으면 = RED / 검사는 있는데 어느 md도 선언 안 하면 = 경고). **전부 GREEN 전엔 이 단계 "완료" 금지.**

- [G6-LINKS] 내부링크 orphan 0(홈 제외 전 라우트 inbound 1+) | auto
- [G6-IMGUSE] keep 이미지 배치 또는 미사용 사유 명시 | manual
- [G6-CONTENT] 서브 본문 권장 분량 충족(overview 800~1500 등) | manual

